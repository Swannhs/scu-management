# DEPRECATED: This compose file is intentionally minimal.
# Source of truth: docker/docker-compose.yml
# Start the platform with:
#   docker compose -f docker/docker-compose.yml up --build
services: {}
