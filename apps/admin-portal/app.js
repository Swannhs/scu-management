const app=document.querySelector('#app');let token=sessionStorage.getItem('scu_admin_token'),route='departments';const api=async(p,o={})=>{const r=await fetch(`/admin/api${p}`,{...o,headers:{'content-type':'application/json',authorization:`Bearer ${token}`,...o.headers}});const b=await r.json().catch(()=>[]);if(!r.ok)throw Error(b.message||'Request failed');return b.data||b};const endpoints={departments:'/v1/admindepartments',programs:'/v1/adminprograms',terms:'/v1/adminterms'};function login(e=''){app.innerHTML=`<section class="login"><div class="card"><h1>SCU Admin</h1><p class="muted">Sign in with a Keycloak administrator account.</p>${e?`<p>${e}</p>`:''}<form id="f"><input name="email" placeholder="Email" required><input name="password" type="password" placeholder="Password" required><button>Sign in</button></form></div></section>`;f.onsubmit=async x=>{x.preventDefault();const d=new FormData(f),b=new URLSearchParams({client_id:'scu-portal',grant_type:'password',username:d.get('email'),password:d.get('password')});try{const r=await fetch('http://localhost:8080/realms/scu/protocol/openid-connect/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body:b}),j=await r.json();if(!r.ok)throw Error(j.error_description);token=j.access_token;sessionStorage.setItem('scu_admin_token',token);render()}catch(z){login(z.message)}}}async function render(){if(!token)return login();let rows=[];try{rows=await api(endpoints[route])}catch{};app.innerHTML=`<div class="shell"><aside class="side"><h2>SCU Admin</h2>${Object.keys(endpoints).map(x=>`<button data-r="${x}">${x}</button>`).join('')}</aside><main class="main"><button class="logout" id="out">Sign out</button><h1>${route}</h1><div class="grid"><section class="panel"><h3>Add ${route.slice(0,-1)}</h3><form id="create"><input name="name" placeholder="Name" required><input name="code" placeholder="Code"><button>Create</button></form></section><section class="panel"><h3>Configured ${route}</h3>${Array.isArray(rows)&&rows.length?rows.map(x=>`<div class="row"><strong>${x.name||x.code||x.id}</strong><span class="muted">${x.code||''}</span></div>`).join(''):'<p class="muted">No records available.</p>'}</section></div></main></div>`;document.querySelectorAll('[data-r]').forEach(b=>b.onclick=()=>{route=b.dataset.r;render()});out.onclick=()=>{sessionStorage.clear();token=null;login()};create.onsubmit=async e=>{e.preventDefault();try{await api(endpoints[route],{method:'POST',body:JSON.stringify(Object.fromEntries(new FormData(create)))});render()}catch(x){alert(x.message)}}}render();
