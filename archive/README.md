# Archived Components

This directory contains services and assets that are no longer part of the active SCU Management runtime.

- `api-gateway`: retired NestJS edge gateway, archived after the move to Traefik plus `services/edge-auth-service`.
